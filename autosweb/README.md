# autosweb → bymy (Vercel bridge)

A Vercel projekt Root Directory-ja történelmileg `autosweb`.
Ez a mappa **buildkor** a `../bymy/public` tartalmát másolja ki.

Ne szerkeszd itt a weboldalt — a forrás: `bymy/`.
